# ============================================================================
# CONFIGURACIÓN DE ENTORNOS - CAMR RANK
# ============================================================================

# ============================================================================
# DESARROLLO LOCAL (.env)
# ============================================================================


# Autenticación
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="tu-secreto-local-aqui"

AUTH_TRUST_HOST=true

# OAuth Debug (opcional)
# ALLOW_LOCAL_OAUTH_DEBUG="true"

# Google OAuth (desarrollo)
GOOGLE_CLIENT_ID="tu-google-client-id-desarrollo"
GOOGLE_CLIENT_SECRET="tu-google-client-secret-desarrollo"

# Almacenamiento de imágenes (desarrollo local)
IMAGE_STORAGE_PATH="./public/images"
IMAGE_STORAGE_URL="http://localhost:3000/images"

# ============================================================================
# PRODUCCIÓN VERCEL
# ============================================================================

# Base de datos Vercel (se configura automáticamente)
# DATABASE_URL="postgresql://..." (configurado por Vercel)

# Autenticación
# NEXTAUTH_URL="https://tu-app.vercel.app" (configurado por Vercel)
# NEXTAUTH_SECRET="..." (configurado por Vercel)

# Google OAuth (producción)
# GOOGLE_CLIENT_ID="..." (configurado por Vercel)
# GOOGLE_CLIENT_SECRET="..." (configurado por Vercel)

# Almacenamiento de imágenes (Vercel Blob)
# CARM_BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..." (configurado por Vercel)

# Almacenamiento de imágenes local (fallback)
# IMAGE_STORAGE_PATH="/tmp" (temporal para Vercel)
# IMAGE_STORAGE_URL="https://tu-app.vercel.app/images"

# ============================================================================
# CONFIGURACIÓN COMÚN
# ============================================================================

# Entorno
NODE_ENV="development" # "development" | "production"

# Configuración de la aplicación
APP_NAME="CAMR Rank"
APP_VERSION="0.2.0-SNAPSHOT"

# Configuración de imágenes
MAX_IMAGE_SIZE=5242880 # 5MB en bytes
ALLOWED_IMAGE_TYPES="image/jpeg,image/png,image/webp"
IMAGE_QUALITY=85 # Calidad de compresión (1-100)

# Configuración de limpieza
# No hay intervalos - limpieza inmediata al aprobar/rechazar

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================

# 1. DESARROLLO LOCAL:
#    - Copia este archivo a .env
#    - Configura tu base de datos PostgreSQL local
#    - Configura Google OAuth para desarrollo

# 2. PRODUCCIÓN VERCEL:
#    - Las variables se configuran en el dashboard de Vercel
#    - DATABASE_URL se configura automáticamente al conectar PostgreSQL
#    - Las imágenes se guardan en /public/images (persistente)
#    - NEXTAUTH_URL se configura automáticamente

# 3. GITIGNORE:
#    - .env NO se sube a Git
#    - Solo env.example se sube como referencia

# NextAuth
NEXTAUTH_URL=
NEXTAUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Database URLs (formato estándar Vercel/Prisma)
# En producción, configurar en Vercel → Settings → Environment Variables
# DATABASE_URL y DIRECT_URL con credenciales reales
# 
# Opción A: Schema específico (ej: carm)
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name?schema=carm
# DIRECT_URL=postgresql://username:password@localhost:5432/database_name?schema=carm
#
# Opción B: Schema por defecto (public)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name
DIRECT_URL=postgresql://username:password@localhost:5432/database_name

# Vercel Blob Storage (para imágenes de juegos)
# En producción, configurar en Vercel → Storage → Blob
CARM_CARM_BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xDW0UcmfAvqYQPEh_ndz3gFikPFmqnZmRnsBsAW69ocRNGS

# Database Pool Configuration
DB_POOL_MAX=20
DB_POOL_MIN=2
DB_POOL_TIMEOUT=60000
DB_CONNECT_TIMEOUT=60000
DB_KEEP_ALIVE_INTERVAL=30000

# Crypto (clave privada para desencriptar contraseñas)
PRIVATE_KEY=MIIEvgIBADANBgkqhkiG9w0BAQEFAASCBKgwggSkAgEAAoIBAQC...

# ============================================================================
# CONFIGURACIÓN DE OAUTH CON GOOGLE
# ============================================================================

#### Google OAuth 2.0
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# NextAuth
NEXTAUTH_SECRET=tu_nextauth_secret_aqui_muy_largo_y_seguro
NEXTAUTH_URL=http://localhost:3000

# ============================================================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================================================

# Puerto del servidor
PORT=3000

# Auth bootstrap
# Owner configuration (automatically created on first run)
OWNER_EMAIL=nikopfleger@gmail.com
OWNER_NAME=Nicolas Pfleger

# ============================================================================
# VERCEL BLOB STORAGE
# ============================================================================

# Vercel Blob (para imágenes en producción)
CARM_BLOB_READ_WRITE_TOKEN=vercel_blob_rw_your_token_here